// Re-export from interfaces.ts for backward compatibility
export type {
  Booking,
  BookingFormData,
  TimeSlot,
  DayAvailability,
} from "./interfaces";

export type ViewMode = "list" | "calendar";
